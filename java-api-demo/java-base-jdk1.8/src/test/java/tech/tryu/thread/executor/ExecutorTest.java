package tech.tryu.thread.executor;

import org.junit.jupiter.api.Test;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import java.util.concurrent.*;

public class ExecutorTest {

    private static final Logger logger = LoggerFactory.getLogger(ExecutorTest.class);

    /**
     * 1. 是如何实现定时运行的
     *
     */
    @Test
    public void scheduledThreadPoolExecutorTest() throws ExecutionException, InterruptedException, TimeoutException {
    }
}
